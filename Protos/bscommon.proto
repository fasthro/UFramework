syntax = "proto3";
package PB_BSCommon;

// 用户
message User {
  int64 userId = 1;
  string userName = 2;
}

// 帧->输入命令
message FrameInputCmd {
  int32 num = 1;
}

// 帧-> 输入
message FrameInput {
  int32 tick = 1;
  repeated FrameInputCmd inputCmds = 2;
}